import { getCurrentSession } from '@/lib/auth';
import { query } from '@/lib/db';
export const dynamic='force-dynamic';
export default async function ServicesPage(){const s=await getCurrentSession();if(!s)return null;const services=await query<{id:string;name:string;sku:string|null;unit:string;default_price:string;active:boolean}>(`SELECT id,name,sku,unit,default_price::text,active FROM service_catalog WHERE organization_id=$1 ORDER BY name`,[s.organization_id]);const money=(v:string)=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v));return <><div className="page-head"><div><h1>Serviços</h1><p>Catálogo padronizado para propostas e ordens.</p></div></div><div className="grid-2"><section className="card"><h2>Novo serviço</h2><form method="post" action="/api/services"><div className="field"><label>Nome</label><input className="input" name="name" required/></div><div className="field"><label>SKU</label><input className="input" name="sku"/></div><div className="field"><label>Unidade</label><input className="input" name="unit" defaultValue="UN"/></div><div className="field"><label>Preço padrão</label><input className="input" type="number" name="defaultPrice" min="0" step="0.01" defaultValue="0"/></div><div className="field"><label>Descrição</label><textarea className="textarea" name="description"/></div><div className="form-actions"><button className="btn">Cadastrar serviço</button></div></form></section><section className="card"><h2>Catálogo</h2><div className="table-wrap"><table className="table"><thead><tr><th>Serviço</th><th>SKU</th><th>Un.</th><th>Preço</th><th>Status</th></tr></thead><tbody>{services.rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.sku||'—'}</td><td>{x.unit}</td><td>{money(x.default_price)}</td><td><span className="badge green">{x.active?'ATIVO':'INATIVO'}</span></td></tr>)}</tbody></table></div></section></div></>}
